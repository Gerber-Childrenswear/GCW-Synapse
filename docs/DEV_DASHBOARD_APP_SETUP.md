# Dev Dashboard app (optional / unused for gcw-dev)

gcw-dev is connected to the **Partners** app:

- Client ID: `7d011b70562512bd84b85bd3f9a6e68d`
- Worker secrets restored from prior Cloudflare/setup history

The Dev Dashboard custom app (`ad45451a4c49376bdeae4dae0f3ac26a`) is **not** the installed storefront app. Keep it around only if you later want a second install path; do not point Worker secrets at it while the Partners app is live.
